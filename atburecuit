<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ATBU Non‑Teaching Staff Recruitment 2025 | Application Portal</title>

    <!-- SEO / Description / Social Sharing -->
    <meta
      name="description"
      content="Apply now for ATBU’s 2025 Non‑Teaching Staff Recruitment. Submit your application and complete payment to secure processing. Application closes Nov 15, 2025."
    />
    <meta
      name="keywords"
      content="ATBU recruitment, ATBU non‑teaching staff, ATBU application, recruitment portal, Abubakar Tafawa Balewa University"
    />
    <meta name="author" content="ATBU Recruitment Portal" />

    <!-- Open Graph / Social Media -->
    <meta
      property="og:title"
      content="ATBU 2025 Non‑Teaching Staff Recruitment Portal"
    />
    <meta
      property="og:description"
      content="Apply for Non‑Teaching positions at ATBU. Application closes Nov 15, 2025."
    />
    <meta
      property="og:image"
      content="https://upload.wikimedia.org/wikipedia/en/6/6e/Abubakar_Tafawa_Balewa_University_%28crest%29.png"
    />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://your‑portal‑url.example.com" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="ATBU 2025 Non‑Teaching Recruitment" />
    <meta
      name="twitter:description"
      content="Apply now for ATBU’s Non‑Teaching positions. Payment required to complete registration by Nov 15, 2025."
    />
    <meta
      name="twitter:image"
      content="https://upload.wikimedia.org/wikipedia/en/6/6e/Abubakar_Tafawa_Balewa_University_%28crest%29.png"
    />

    <!-- Favicon -->
    <link
      rel="icon"
      type="image/png"
      href="https://upload.wikimedia.org/wikipedia/en/6/6e/Abubakar_Tafawa_Balewa_University_%28crest%29.png"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=DM+Serif+Display&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --primary: #1a365d;
        --secondary: #2d3748;
        --accent: #3182ce;
        --accent-glow: rgba(49, 130, 206, 0.4);
        --light: #f7fafc;
        --dark: #1a202c;
        --success: #38a169;
        --warning: #ecc94b;
        --error: #e53e3e;
        --glass-bg: rgba(255, 255, 255, 0.08);
        --glass-border: rgba(255, 255, 255, 0.18);
        --shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        --transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Inter", sans-serif;
        background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
        color: var(--light);
        line-height: 1.6;
        overflow-x: hidden;
        min-height: 100vh;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 20px;
      }

      /* Header & Navigation */
      header {
        padding: 20px 0;
        position: sticky;
        top: 0;
        z-index: 100;
        backdrop-filter: blur(10px);
        background: rgba(15, 23, 42, 0.8);
        border-bottom: 1px solid var(--glass-border);
      }

      .header-content {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      .logo-container {
        display: flex;
        align-items: center;
        gap: 15px;
      }

      .logo {
        width: 60px;
        height: 60px;
        background: linear-gradient(135deg, var(--accent), #63b3ed);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 0 20px var(--accent-glow);
      }

      .logo i {
        font-size: 28px;
        color: white;
      }

      .logo-text h1 {
        font-size: 1.5rem;
        font-weight: 700;
        line-height: 1.2;
      }

      .logo-text p {
        font-size: 0.85rem;
        opacity: 0.8;
      }

      /* Hero Section */
      .hero {
        padding: 80px 0 60px;
        text-align: center;
        position: relative;
        overflow: hidden;
      }

      .hero::before {
        content: "";
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(
          circle,
          rgba(49, 130, 206, 0.1) 0%,
          rgba(15, 23, 42, 0) 70%
        );
        z-index: -1;
      }

      .hero h1 {
        font-family: "DM Serif Display", serif;
        font-size: 3.5rem;
        margin-bottom: 15px;
        background: linear-gradient(to right, #fff, #a0c8f8);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        text-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
      }

      .hero p {
        font-size: 1.2rem;
        max-width: 600px;
        margin: 0 auto 30px;
        opacity: 0.9;
      }

      .deadline {
        display: inline-block;
        background: rgba(239, 68, 68, 0.2);
        border: 1px solid rgba(239, 68, 68, 0.4);
        padding: 8px 20px;
        border-radius: 50px;
        font-weight: 500;
        margin-bottom: 40px;
        backdrop-filter: blur(5px);
      }

      /* Notification Banner */
      .notification {
        background: linear-gradient(90deg, var(--accent), #4c7cb3);
        padding: 12px 0;
        margin-bottom: 40px;
        position: relative;
        overflow: hidden;
      }

      .notification-content {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        animation: scrollText 20s linear infinite;
      }

      @keyframes scrollText {
        0% {
          transform: translateX(100%);
        }
        100% {
          transform: translateX(-100%);
        }
      }

      /* Main Content */
      .main-content {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 40px;
        margin-bottom: 60px;
      }

      @media (max-width: 968px) {
        .main-content {
          grid-template-columns: 1fr;
        }
      }

      /* Form Styling */
      .form-container,
      .payment-info {
        background: var(--glass-bg);
        backdrop-filter: blur(10px);
        border: 1px solid var(--glass-border);
        border-radius: 20px;
        padding: 30px;
        box-shadow: var(--shadow);
        transition: var(--transition);
      }

      .form-container:hover,
      .payment-info:hover {
        box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
        transform: translateY(-5px);
      }

      .section-title {
        font-size: 1.5rem;
        margin-bottom: 25px;
        padding-bottom: 10px;
        border-bottom: 1px solid var(--glass-border);
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .section-title i {
        color: var(--accent);
      }

      .form-group {
        margin-bottom: 20px;
      }

      label {
        display: block;
        margin-bottom: 8px;
        font-weight: 500;
      }

      input,
      select {
        width: 100%;
        padding: 14px 18px;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid var(--glass-border);
        border-radius: 12px;
        color: var(--light);
        font-size: 1rem;
        transition: var(--transition);
      }

      input:focus,
      select:focus {
        outline: none;
        border-color: var(--accent);
        box-shadow: 0 0 0 3px var(--accent-glow);
      }

      .file-upload {
        position: relative;
        overflow: hidden;
        display: inline-block;
        width: 100%;
      }

      .file-upload-btn {
        background: rgba(49, 130, 206, 0.2);
        border: 1px dashed var(--accent);
        color: var(--light);
        padding: 14px 18px;
        border-radius: 12px;
        font-size: 1rem;
        font-weight: 500;
        width: 100%;
        text-align: center;
        cursor: pointer;
        transition: var(--transition);
      }

      .file-upload-btn:hover {
        background: rgba(49, 130, 206, 0.3);
      }

      .file-upload input[type="file"] {
        position: absolute;
        left: 0;
        top: 0;
        opacity: 0;
        width: 100%;
        height: 100%;
        cursor: pointer;
      }

      .file-list {
        margin-top: 10px;
        font-size: 0.9rem;
        max-height: 120px;
        overflow-y: auto;
      }

      .file-item {
        display: flex;
        justify-content: space-between;
        padding: 8px 0;
        border-bottom: 1px solid var(--glass-border);
      }

      .submit-btn {
        background: linear-gradient(135deg, var(--accent), #4c7cb3);
        color: white;
        border: none;
        padding: 16px 30px;
        font-size: 1.1rem;
        font-weight: 600;
        border-radius: 12px;
        cursor: pointer;
        width: 100%;
        transition: var(--transition);
        margin-top: 10px;
        box-shadow: 0 4px 15px rgba(49, 130, 206, 0.3);
      }

      .submit-btn:hover {
        transform: translateY(-3px);
        box-shadow: 0 7px 20px rgba(49, 130, 206, 0.4);
      }

      .submit-btn:active {
        transform: translateY(0);
      }

      /* Payment Info */
      .payment-details {
        background: rgba(239, 68, 68, 0.1);
        border: 1px solid rgba(239, 68, 68, 0.3);
        border-radius: 12px;
        padding: 20px;
        margin: 20px 0;
      }

      .payment-details h4 {
        color: #feb2b2;
        margin-bottom: 15px;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .bank-details {
        background: rgba(56, 161, 105, 0.1);
        border: 1px solid rgba(56, 161, 105, 0.3);
        border-radius: 12px;
        padding: 20px;
        margin: 20px 0;
      }

      .bank-details h4 {
        color: #9ae6b4;
        margin-bottom: 15px;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .detail-item {
        display: flex;
        justify-content: space-between;
        margin-bottom: 10px;
        padding-bottom: 10px;
        border-bottom: 1px solid var(--glass-border);
      }

      /* Footer */
      footer {
        background: rgba(15, 23, 42, 0.9);
        border-top: 1px solid var(--glass-border);
        padding: 40px 0 20px;
        margin-top: 40px;
      }

      .footer-content {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 30px;
        margin-bottom: 30px;
      }

      .footer-section h3 {
        margin-bottom: 20px;
        font-size: 1.2rem;
        color: #a0c8f8;
      }

      .disclaimer {
        font-size: 0.9rem;
        opacity: 0.8;
        line-height: 1.5;
      }

      .contact-info p {
        margin-bottom: 10px;
        display: flex;
        align-items: flex-start;
        gap: 10px;
      }

      .copyright {
        text-align: center;
        padding-top: 20px;
        border-top: 1px solid var(--glass-border);
        font-size: 0.9rem;
        opacity: 0.7;
      }

      /* Modal */
      .modal {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(15, 23, 42, 0.9);
        display: flex;
        align-items: flex-end;
        justify-content: center;
        z-index: 1000;
        opacity: 0;
        visibility: hidden;
        transition: var(--transition);
      }

      .modal.active {
        opacity: 1;
        visibility: visible;
      }

      .modal-content {
        background: var(--glass-bg);
        backdrop-filter: blur(20px);
        border: 1px solid var(--glass-border);
        border-radius: 20px 20px 0 0;
        width: 100%;
        max-width: 600px;
        padding: 30px;
        transform: translateY(100%);
        transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
      }

      .modal.active .modal-content {
        transform: translateY(0);
      }

      .modal-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
      }

      .modal-title {
        font-size: 1.5rem;
        font-weight: 600;
      }

      .close-modal {
        background: none;
        border: none;
        color: var(--light);
        font-size: 1.5rem;
        cursor: pointer;
        transition: var(--transition);
      }

      .close-modal:hover {
        color: var(--accent);
      }

      .application-summary {
        background: rgba(255, 255, 255, 0.05);
        border-radius: 12px;
        padding: 20px;
        margin-bottom: 20px;
      }

      .summary-item {
        display: flex;
        justify-content: space-between;
        margin-bottom: 10px;
        padding-bottom: 10px;
        border-bottom: 1px solid var(--glass-border);
      }

      .status-badge {
        display: inline-block;
        background: rgba(245, 158, 11, 0.2);
        color: #f6ad55;
        padding: 5px 15px;
        border-radius: 50px;
        font-size: 0.9rem;
        font-weight: 500;
        margin-top: 10px;
      }

      /* Responsive Adjustments */
      @media (max-width: 768px) {
        .hero h1 {
          font-size: 2.5rem;
        }

        .main-content {
          gap: 20px;
        }

        .form-container,
        .payment-info {
          padding: 20px;
        }
      }

      @media (max-width: 480px) {
        .hero h1 {
          font-size: 2rem;
        }

        .hero p {
          font-size: 1rem;
        }

        .header-content {
          flex-direction: column;
          gap: 15px;
        }

        .logo-text {
          text-align: center;
        }
      }

      /* Animation Classes */
      .fade-in {
        animation: fadeIn 0.8s ease forwards;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .slide-in-left {
        animation: slideInLeft 0.8s ease forwards;
      }

      @keyframes slideInLeft {
        from {
          opacity: 0;
          transform: translateX(-30px);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      .slide-in-right {
        animation: slideInRight 0.8s ease forwards;
      }

      @keyframes slideInRight {
        from {
          opacity: 0;
          transform: translateX(30px);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      /* Bubble Background */
      .bubbles {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        overflow: hidden;
      }

      .bubble {
        position: absolute;
        border-radius: 50%;
        background: rgba(49, 130, 206, 0.1);
        animation: float 15s infinite ease-in-out;
      }

      @keyframes float {
        0%,
        100% {
          transform: translateY(0) translateX(0);
        }
        25% {
          transform: translateY(-20px) translateX(10px);
        }
        50% {
          transform: translateY(-40px) translateX(-10px);
        }
        75% {
          transform: translateY(-20px) translateX(-5px);
        }
      }
    </style>
  </head>
  <body>
    <!-- Bubble Background -->
    <div class="bubbles" id="bubbles"></div>

    <!-- Header -->
    <header>
      <div class="container">
        <div class="header-content">
          <div class="logo-container">
            <div class="logo">
              <img
                src="https://upload.wikimedia.org/wikipedia/en/6/6e/Abubakar_Tafawa_Balewa_University_%28crest%29.png"
                alt="ATBU Logo"
                style="width: 40px; height: 40px; border-radius: 50%"
              />
            </div>
            <div class="logo-text">
              <h1>ATBU</h1>
              <p>Abubakar Tafawa Balewa University</p>
            </div>
          </div>
          <div class="header-info">
            <p>2025 Non-Teaching Staff Recruitment</p>
          </div>
        </div>
      </div>
    </header>

    <!-- Hero Section -->
    <section class="hero">
      <div class="container">
        <h1 class="fade-in">Join Our Prestigious Institution</h1>
        <p class="fade-in">
          Shape the future of education with ATBU. We're seeking exceptional
          talent to join our non-teaching staff for the 2025 academic session.
        </p>
        <div class="deadline fade-in">
          <i class="fas fa-clock"></i> Application Closes November 15, 2025
        </div>
      </div>
    </section>

    <!-- Notification Banner -->
    <div class="notification">
      <div class="container">
        <div class="notification-content">
          <i class="fas fa-bullhorn"></i>
          <span
            >ATBU Announces 2025 Staff Recruitment — Portal Closes Nov 15,
            2025</span
          >
        </div>
      </div>
    </div>

    <!-- Main Content -->
    <div class="container">
      <div class="main-content">
        <!-- Application Form -->
        <div class="form-container slide-in-left">
          <h2 class="section-title">
            <i class="fas fa-edit"></i> Application Form
          </h2>
          <form id="applicationForm">
            <div class="form-group">
              <label for="fullName">Full Name</label>
              <input
                type="text"
                id="fullName"
                name="fullName"
                placeholder="Enter your full name"
                required
              />
            </div>

            <div class="form-group">
              <label for="email">Email Address</label>
              <input
                type="email"
                id="email"
                name="email"
                placeholder="Enter your email address"
                required
              />
            </div>

            <div class="form-group">
              <label for="phone">Phone Number</label>
              <input
                type="tel"
                id="phone"
                name="phone"
                placeholder="Enter your phone number"
                required
              />
            </div>

            <div class="form-group">
              <label for="position">Position Applying For</label>
              <select id="position" name="position" required>
                <option value="">Select a position</option>
                <option value="Assistant Lecturer (CONUASS 02)">
                  Assistant Lecturer (CONUASS 02)
                </option>
                <option value="Lecturer II (CONUASS 03)">
                  Lecturer II (CONUASS 03)
                </option>
                <option value="Lecturer I (CONUASS 04)">
                  Lecturer I (CONUASS 04)
                </option>
                <option value="Senior Lecturer (CONUASS 05)">
                  Senior Lecturer (CONUASS 05)
                </option>
                <option value="Laboratory Technologist I (CONTISS 08)">
                  Laboratory Technologist I (CONTISS 08)
                </option>
                <option value="Laboratory Technologist II (CONTISS 07)">
                  Laboratory Technologist II (CONTISS 07)
                </option>
              </select>
            </div>

            <div class="form-group">
              <label for="department">Faculty / Department</label>
              <select id="department" name="department" required>
                <option value="">Select faculty/department</option>
                <option value="Faculty of Engineering">
                  Faculty of Engineering
                </option>
                <option value="Faculty of Science">Faculty of Science</option>
                <option value="Faculty of Management Sciences">
                  Faculty of Management Sciences
                </option>
                <option value="Faculty of Education">
                  Faculty of Education
                </option>
                <option value="Faculty of Agriculture">
                  Faculty of Agriculture
                </option>
                <option value="Faculty of Environmental Technology">
                  Faculty of Environmental Technology
                </option>
                <option value="Registry">Registry</option>
                <option value="Bursary">Bursary</option>
                <option value="Library">Library</option>
                <option value="Health Services">Health Services</option>
              </select>
            </div>

            <div class="form-group">
              <label>Upload Credentials</label>
              <div class="file-upload">
                <div class="file-upload-btn">
                  <i class="fas fa-cloud-upload-alt"></i> Click to upload files
                  (CV, Application Letter, Certificates)
                </div>
                <input
                  type="file"
                  id="credentials"
                  name="credentials"
                  multiple
                />
              </div>
              <div class="file-list" id="fileList"></div>
            </div>

            <button type="submit" class="submit-btn">
              <i class="fas fa-check-circle"></i> Complete Registration
            </button>
          </form>
        </div>

        <!-- Payment Information -->
        <div class="payment-info slide-in-right">
          <h2 class="section-title">
            <i class="fas fa-credit-card"></i> Payment Information
          </h2>

          <div class="payment-details">
            <h4><i class="fas fa-exclamation-circle"></i> Important Notice</h4>
            <p>
              A non-refundable fee of <strong>₦2,500</strong> is required to
              complete your application. Your application will not be processed
              until payment is confirmed.
            </p>
          </div>

          <div class="bank-details">
            <h4><i class="fas fa-university"></i> Account Details</h4>
            <div class="detail-item">
              <span>Bank:</span>
              <span>Nombank MFB</span>
            </div>
            <div class="detail-item">
              <span>Account Number:</span>
              <span>7462721720</span>
            </div>
            <div class="detail-item">
              <span>Account Name:</span>
              <span>ATBU Recruitment Off (JUSTINA NZERIBE)</span>
            </div>
          </div>

          <div class="additional-info">
            <h4><i class="fas fa-info-circle"></i> Application Process</h4>
            <p>
              After completing your application, you'll receive a confirmation
              with further instructions. Only shortlisted candidates will be
              contacted for interviews.
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <footer>
      <div class="container">
        <div class="footer-content">
          <div class="footer-section">
            <h3>Disclaimer</h3>
            <p class="disclaimer">
              Only shortlisted applicants will be contacted for interviews.
              Conditions of service apply as in all Nigerian Federal
              Universities. All applications are subject to verification and
              approval by the university management.
            </p>
          </div>

          <div class="footer-section">
            <h3>Contact Information</h3>
            <div class="contact-info">
              <p>
                <i class="fas fa-map-marker-alt"></i> The Registrar,<br />Abubakar
                Tafawa Balewa University<br />P.M.B 0248, Gubi Campus,<br />Bauchi,
                Bauchi State, Nigeria.
              </p>
              <p><i class="fas fa-envelope"></i> recruitment@atbu.edu.ng</p>
              <p><i class="fas fa-phone"></i> +234 (0) 803 123 4567</p>
            </div>
          </div>

          <div class="footer-section">
            <h3>Quick Links</h3>
            <ul>
              <li><a href="#">ATBU Official Website</a></li>
              <li><a href="#">Academic Programs</a></li>
              <li><a href="#">Staff Portal</a></li>
              <li><a href="#">Student Portal</a></li>
            </ul>
          </div>
        </div>

        <div class="copyright">
          <p>
            &copy; 2025 Abubakar Tafawa Balewa University. All Rights Reserved.
          </p>
        </div>
      </div>
    </footer>

    <!-- Confirmation Modal -->
    <div class="modal" id="confirmationModal">
      <div class="modal-content">
        <div class="modal-header">
          <h3 class="modal-title">Application Submitted</h3>
          <button class="close-modal" id="closeModal">&times;</button>
        </div>

        <div class="application-summary">
          <h4>Application Summary</h4>
          <div class="summary-item">
            <span>Name:</span>
            <span id="summaryName"></span>
          </div>
          <div class="summary-item">
            <span>Email:</span>
            <span id="summaryEmail"></span>
          </div>
          <div class="summary-item">
            <span>Phone:</span>
            <span id="summaryPhone"></span>
          </div>
          <div class="summary-item">
            <span>Position:</span>
            <span id="summaryPosition"></span>
          </div>
          <div class="summary-item">
            <span>Department:</span>
            <span id="summaryDepartment"></span>
          </div>

          <div class="status-badge">
            <i class="fas fa-clock"></i> Application Status: Pending Payment
            Confirmation
          </div>
        </div>

        <div class="payment-reminder">
          <h4>Payment Instructions</h4>
          <p>
            Please complete your payment using the bank details provided and
            send proof of payment to <strong>payments@atbu.edu.ng</strong> with
            your application ID.
          </p>

          <div class="bank-details">
            <div class="detail-item">
              <span>Bank:</span>
              <span>Nombank MFB</span>
            </div>
            <div class="detail-item">
              <span>Account Number:</span>
              <span>7462721720</span>
            </div>
            <div class="detail-item">
              <span>Account Name:</span>
              <span>ATBU Recruitment Off (JUSTINA NZERIBE)</span>
            </div>
            <div class="detail-item">
              <span>Amount:</span>
              <span>₦2,500</span>
            </div>
          </div>
        </div>

        <button class="submit-btn" id="closeModalBtn">
          <i class="fas fa-check"></i> Understood
        </button>
      </div>
    </div>

    <script>
      // Create bubble background
      function createBubbles() {
        const bubblesContainer = document.getElementById("bubbles");
        const bubbleCount = 15;

        for (let i = 0; i < bubbleCount; i++) {
          const bubble = document.createElement("div");
          bubble.classList.add("bubble");

          // Random size between 20 and 100px
          const size = Math.random() * 80 + 20;
          bubble.style.width = `${size}px`;
          bubble.style.height = `${size}px`;

          // Random position
          bubble.style.left = `${Math.random() * 100}%`;
          bubble.style.top = `${Math.random() * 100}%`;

          // Random animation delay
          bubble.style.animationDelay = `${Math.random() * 15}s`;

          bubblesContainer.appendChild(bubble);
        }
      }

      // Handle file upload display
      document
        .getElementById("credentials")
        .addEventListener("change", function (e) {
          const fileList = document.getElementById("fileList");
          fileList.innerHTML = "";

          if (this.files.length > 0) {
            for (let i = 0; i < this.files.length; i++) {
              const fileItem = document.createElement("div");
              fileItem.classList.add("file-item");

              const fileName = document.createElement("span");
              fileName.textContent = this.files[i].name;

              const fileSize = document.createElement("span");
              fileSize.textContent = formatFileSize(this.files[i].size);

              fileItem.appendChild(fileName);
              fileItem.appendChild(fileSize);
              fileList.appendChild(fileItem);
            }
          } else {
            fileList.innerHTML = "<p>No files selected</p>";
          }
        });

      // Format file size
      function formatFileSize(bytes) {
        if (bytes === 0) return "0 Bytes";
        const k = 1024;
        const sizes = ["Bytes", "KB", "MB", "GB"];
        const i = Math.floor(Math.log(bytes) / Math.log(k));
        return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + " " + sizes[i];
      }

      // Handle form submission
      document
        .getElementById("applicationForm")
        .addEventListener("submit", function (e) {
          e.preventDefault();

          // Get form values
          const formData = {
            name: document.getElementById("fullName").value,
            email: document.getElementById("email").value,
            phone: document.getElementById("phone").value,
            position: document.getElementById("position").value,
            department: document.getElementById("department").value,
          };

          // Update modal with form data
          document.getElementById("summaryName").textContent = formData.name;
          document.getElementById("summaryEmail").textContent = formData.email;
          document.getElementById("summaryPhone").textContent = formData.phone;
          document.getElementById("summaryPosition").textContent =
            formData.position;
          document.getElementById("summaryDepartment").textContent =
            formData.department;

          // Show modal
          document.getElementById("confirmationModal").classList.add("active");

          // Save to localStorage
          localStorage.setItem("atbuApplication", JSON.stringify(formData));
        });

      // Close modal
      document
        .getElementById("closeModal")
        .addEventListener("click", function () {
          document
            .getElementById("confirmationModal")
            .classList.remove("active");
        });

      document
        .getElementById("closeModalBtn")
        .addEventListener("click", function () {
          document
            .getElementById("confirmationModal")
            .classList.remove("active");
        });

      // Load saved form data if exists
      window.addEventListener("DOMContentLoaded", function () {
        createBubbles();

        const savedData = localStorage.getItem("atbuApplication");
        if (savedData) {
          const formData = JSON.parse(savedData);
          document.getElementById("fullName").value = formData.name || "";
          document.getElementById("email").value = formData.email || "";
          document.getElementById("phone").value = formData.phone || "";
          document.getElementById("position").value = formData.position || "";
          document.getElementById("department").value =
            formData.department || "";
        }

        // Add animation classes to elements
        const animatedElements = document.querySelectorAll(
          ".fade-in, .slide-in-left, .slide-in-right"
        );
        animatedElements.forEach((el, index) => {
          el.style.animationDelay = `${index * 0.1}s`;
        });
      });
    </script>
  </body>
</html>
